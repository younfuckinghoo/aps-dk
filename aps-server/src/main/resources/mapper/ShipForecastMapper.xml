<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aps.yinghai.mapper.ShipForecastMapper">

    <select id="checkMaxCreateTime" resultType="java.time.LocalDateTime">
        select max(CREATE_DATE) from ALG_SHIP_FORECAST
    </select>
</mapper>
